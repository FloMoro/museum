<p id="notice"><%= notice %></p>

<h1>Artistes</h1>

<table>
  <thead>
    <tr>
      <th>Prénom</th>
      <th>Nom</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artists.each do |artist| %>
      <tr>
        <td><%= artist.first_name %></td>
        <td><%= artist.last_name %></td>
        <td><%= link_to 'Montrer', artist %></td>
        <td><%= link_to 'Modifier', edit_artist_path(artist) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvel artiste', new_artist_path %>
